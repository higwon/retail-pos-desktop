<UserControl x:Class="RetailPOS.Desktop.Controls.NavigationHost"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="720"
             d:DesignWidth="1280">
    <UserControl.Resources>
        <SolidColorBrush x:Key="NavigationRailBorderBrush" Color="#FF334155"/>
        <Style x:Key="NavigationRailIconStyle"
               TargetType="Path"
               BasedOn="{StaticResource IconPathStyle}">
            <Setter Property="Width" Value="20"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="StrokeThickness" Value="1.7"/>
            <Setter Property="Stroke" Value="White"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="NavigationRailButtonStyle"
               TargetType="Button"
               BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="{StaticResource NavigationRailBorderBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="MinHeight" Value="56"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource AppBackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="190"/>
        </Grid.ColumnDefinitions>

        <Grid x:Name="ContentRoot"
              Grid.ColumnSpan="2"
              Background="{StaticResource AppBackgroundBrush}"/>

        <Border x:Name="DemoNavigation"
                Grid.Column="1"
                Visibility="Collapsed"
                Background="{StaticResource BrandDarkBrush}"
                BorderBrush="#FF334155"
                BorderThickness="1,0,0,0"
                Padding="16,16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Margin="8,2,8,18">
                    <TextBlock Text="Retail POS"
                               Foreground="White"
                               FontSize="18"
                               FontWeight="SemiBold"/>
                    <TextBlock Text="Cashier menu"
                               Foreground="#FFCBD5E1"
                               FontSize="11"
                               Margin="0,4,0,0"/>
                </StackPanel>

                <StackPanel Grid.Row="1">
                    <Button x:Name="RegisterNavigationButton"
                            Style="{StaticResource NavigationRailButtonStyle}"
                            Click="OnShowRegister">
                        <Grid Margin="8,0"><Grid.ColumnDefinitions><ColumnDefinition Width="28"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><Path Data="{StaticResource IconCardGeometry}" Style="{StaticResource NavigationRailIconStyle}"/><TextBlock Grid.Column="1" Text="Register" Foreground="White" Margin="8,0,0,0" VerticalAlignment="Center" TextWrapping="NoWrap"/></Grid>
                    </Button>
                    <Button x:Name="ProductSearchNavigationButton"
                            Style="{StaticResource NavigationRailButtonStyle}"
                            Click="OnShowProductSearch">
                        <Grid Margin="8,0"><Grid.ColumnDefinitions><ColumnDefinition Width="28"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><Path Data="{StaticResource IconSearchGeometry}" Style="{StaticResource NavigationRailIconStyle}"/><TextBlock Grid.Column="1" Text="Product Search" Foreground="White" Margin="8,0,0,0" VerticalAlignment="Center" TextWrapping="NoWrap"/></Grid>
                    </Button>
                    <Button x:Name="ReceiptNavigationButton"
                            Style="{StaticResource NavigationRailButtonStyle}"
                            Click="OnShowReceipts">
                        <Grid Margin="8,0"><Grid.ColumnDefinitions><ColumnDefinition Width="28"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><Path Data="{StaticResource IconReceiptGeometry}" Style="{StaticResource NavigationRailIconStyle}"/><TextBlock Grid.Column="1" Text="Receipts" Foreground="White" Margin="8,0,0,0" VerticalAlignment="Center" TextWrapping="NoWrap"/></Grid>
                    </Button>
                    <Button x:Name="RecoveryNavigationButton"
                            Style="{StaticResource NavigationRailButtonStyle}"
                            Click="OnShowRecovery">
                        <Grid Margin="8,0"><Grid.ColumnDefinitions><ColumnDefinition Width="28"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><Path Data="{StaticResource IconRecoveryGeometry}" Style="{StaticResource NavigationRailIconStyle}"/><TextBlock Grid.Column="1" Text="Recovery" Foreground="White" Margin="8,0,0,0" VerticalAlignment="Center" TextWrapping="NoWrap"/></Grid>
                    </Button>
                    <Button x:Name="DashboardNavigationButton"
                            Style="{StaticResource NavigationRailButtonStyle}"
                            Click="OnShowDashboard">
                        <Grid Margin="8,0"><Grid.ColumnDefinitions><ColumnDefinition Width="28"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><Path Data="{StaticResource IconDashboardGeometry}" Style="{StaticResource NavigationRailIconStyle}"/><TextBlock Grid.Column="1" Text="Dashboard" Foreground="White" Margin="8,0,0,0" VerticalAlignment="Center" TextWrapping="NoWrap"/></Grid>
                    </Button>
                    <Button x:Name="StatusNavigationButton"
                            Style="{StaticResource NavigationRailButtonStyle}"
                            Click="OnShowStatus">
                        <Grid Margin="8,0"><Grid.ColumnDefinitions><ColumnDefinition Width="28"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><Path Data="{StaticResource IconStatusGeometry}" Style="{StaticResource NavigationRailIconStyle}"/><TextBlock Grid.Column="1" Text="Status" Foreground="White" Margin="8,0,0,0" VerticalAlignment="Center" TextWrapping="NoWrap"/></Grid>
                    </Button>
                </StackPanel>

                <StackPanel Grid.Row="3">
                    <Button x:Name="DeviceSimulatorButton"
                            Style="{StaticResource NavigationRailButtonStyle}"
                            Click="OnOpenDeviceSimulator">
                        <Grid Margin="8,0"><Grid.ColumnDefinitions><ColumnDefinition Width="28"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><Path Data="{StaticResource IconDisplayGeometry}" Style="{StaticResource NavigationRailIconStyle}"/><TextBlock Grid.Column="1" Text="Simulator" Foreground="White" Margin="8,0,0,0" VerticalAlignment="Center" TextWrapping="NoWrap"/></Grid>
                    </Button>
                    <Border BorderBrush="#FF475569"
                            BorderThickness="0,1,0,0"
                            Margin="0,5,0,10"/>
                    <Button
                            Style="{StaticResource NavigationRailButtonStyle}"
                            Click="OnSignOut">
                        <Grid Margin="8,0"><Grid.ColumnDefinitions><ColumnDefinition Width="28"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><Path Data="{StaticResource IconSignOutGeometry}" Style="{StaticResource NavigationRailIconStyle}"/><TextBlock Grid.Column="1" Text="Sign out" Foreground="White" Margin="8,0,0,0" VerticalAlignment="Center" TextWrapping="NoWrap"/></Grid>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
