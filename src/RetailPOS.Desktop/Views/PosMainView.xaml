<UserControl x:Class="RetailPOS.Desktop.Views.PosMainView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
 <Grid Background="{StaticResource AppBackgroundBrush}">
  <Grid.RowDefinitions><RowDefinition Height="88"/><RowDefinition Height="*"/></Grid.RowDefinitions>
  <Border Style="{StaticResource CashierPageHeaderStyle}">
   <Grid>
    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
     <TextBlock Text="Register" Style="{StaticResource CashierPageTitleTextStyle}"/>
     <Border Background="{Binding ConnectivityBadgeBackground}" CornerRadius="12" Padding="10,4" Margin="14,0,0,0"><TextBlock Text="{Binding ConnectivityText}" Foreground="{Binding ConnectivityBadgeForeground}" FontSize="12"/></Border>
     <TextBlock Text="{Binding DeviceSummaryText}" Style="{StaticResource SecondaryTextBlockStyle}" VerticalAlignment="Center" Margin="14,0,0,0" MaxWidth="210" TextTrimming="CharacterEllipsis" ToolTip="{Binding DeviceSummaryText}"/>
     <TextBlock x:Name="ScannerStatusText"
                Text="{Binding BarcodeMessage}"
                Foreground="{StaticResource DangerBrush}"
                VerticalAlignment="Center"
                Margin="14,0,0,0"
                MaxWidth="260"
                TextWrapping="NoWrap"
                TextTrimming="CharacterEllipsis"
                AutomationProperties.LiveSetting="Assertive"
                ToolTip="{Binding BarcodeMessage}">
      <TextBlock.Style>
       <Style TargetType="TextBlock">
        <Setter Property="Visibility" Value="Collapsed"/>
        <Style.Triggers>
         <DataTrigger Binding="{Binding HasBarcodeMessage}" Value="True">
          <Setter Property="Visibility" Value="Visible"/>
         </DataTrigger>
        </Style.Triggers>
       </Style>
      </TextBlock.Style>
     </TextBlock>
    </StackPanel>
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
      <Button Style="{StaticResource SecondaryButtonStyle}" MinHeight="34" Padding="12,4" Margin="0,0,10,0" Click="OnOpenCustomerDisplay"><StackPanel Orientation="Horizontal"><Path Data="{StaticResource IconDisplayGeometry}" Style="{StaticResource IconPathStyle}" Margin="0,0,8,0"/><TextBlock Text="Customer Display"/></StackPanel></Button>
     <StackPanel VerticalAlignment="Center"><TextBlock Text="{Binding CashierText}" MaxWidth="180" TextTrimming="CharacterEllipsis"/><TextBlock Text="{Binding StoreTerminalText}" Style="{StaticResource SecondaryTextBlockStyle}" FontSize="11" MaxWidth="180" TextTrimming="CharacterEllipsis"/></StackPanel>
    </StackPanel>
   </Grid>
  </Border>
  <ContentControl x:Name="CartRegion" Grid.Row="1" Margin="20,16,20,20"/>
 </Grid>
</UserControl>
