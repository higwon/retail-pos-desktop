<UserControl x:Class="RetailPOS.Desktop.Views.PosMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{StaticResource AppBackgroundBrush}">
        <Grid.RowDefinitions><RowDefinition Height="68"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="28,0">
            <Grid>
                <TextBlock Text="Retail POS" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Border Background="#FFDCFCE7" CornerRadius="12" Padding="12,5" Margin="0,0,18,0"><TextBlock Text="Online" Foreground="#FF166534" FontSize="12"/></Border>
                    <Button Content="Customer Display" Style="{StaticResource SecondaryButtonStyle}"
                            MinHeight="34" Padding="12,4" Margin="0,0,18,0"
                            Click="OnOpenCustomerDisplay" />
                    <Button Content="Payment" Style="{StaticResource PrimaryBlueButtonStyle}"
                            MinHeight="34" Padding="12,4" Margin="0,0,8,0"
                            Click="OnOpenPayment" />
                    <Button Content="Receipt" Style="{StaticResource SecondaryButtonStyle}"
                            MinHeight="34" Padding="12,4" Margin="0,0,18,0"
                            Click="OnOpenReceipt" />
                    <TextBlock Text="Cashier A" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
        <Grid Grid.Row="1" Margin="24">
            <Grid.ColumnDefinitions><ColumnDefinition Width="220"/><ColumnDefinition Width="14"/><ColumnDefinition Width="*"/><ColumnDefinition Width="14"/><ColumnDefinition Width="390"/></Grid.ColumnDefinitions>
            <Border Grid.Column="0" Style="{StaticResource PanelBorderStyle}" Padding="20">
                <StackPanel>
                    <TextBlock Text="Categories" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,18"/>
                    <Border Background="{StaticResource SelectionBrush}" CornerRadius="5" Padding="14,10"><TextBlock Text="All Products" Foreground="{StaticResource PrimaryBlueBrush}"/></Border>
                    <TextBlock Text="Beauty" Margin="14,22,0,0"/><TextBlock Text="Hair Care" Margin="14,22,0,0"/><TextBlock Text="Health" Margin="14,22,0,0"/><TextBlock Text="Food" Margin="14,22,0,0"/><TextBlock Text="Promotion" Margin="14,22,0,0"/>
                </StackPanel>
            </Border>
            <ContentControl x:Name="ProductRegion" Grid.Column="2" />
            <ContentControl x:Name="CartRegion" Grid.Column="4" />
        </Grid>
    </Grid>
</UserControl>
