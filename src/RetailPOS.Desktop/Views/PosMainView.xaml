<UserControl x:Class="RetailPOS.Desktop.Views.PosMainView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
 <Grid Background="{StaticResource AppBackgroundBrush}">
  <Grid.RowDefinitions><RowDefinition Height="64"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
  <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="24,0">
   <Grid>
    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
     <TextBlock Text="Register" FontSize="20" FontWeight="SemiBold"/>
     <Border Background="{Binding ConnectivityBadgeBackground}" CornerRadius="12" Padding="10,4" Margin="14,0,0,0"><TextBlock Text="{Binding ConnectivityText}" Foreground="{Binding ConnectivityBadgeForeground}" FontSize="12"/></Border>
     <TextBlock Text="{Binding DeviceSummaryText}" Style="{StaticResource SecondaryTextBlockStyle}" VerticalAlignment="Center" Margin="14,0,0,0" MaxWidth="210" TextTrimming="CharacterEllipsis" ToolTip="{Binding DeviceSummaryText}"/>
    </StackPanel>
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
     <Button Content="Customer Display" Style="{StaticResource SecondaryButtonStyle}" MinHeight="34" Padding="12,4" Margin="0,0,10,0" Click="OnOpenCustomerDisplay"/>
     <Button Content="Receipt" Style="{StaticResource SecondaryButtonStyle}" MinHeight="34" Padding="12,4" Margin="0,0,16,0" Click="OnOpenReceipt"/>
     <StackPanel VerticalAlignment="Center"><TextBlock Text="{Binding CashierText}" MaxWidth="180" TextTrimming="CharacterEllipsis"/><TextBlock Text="{Binding StoreTerminalText}" Style="{StaticResource SecondaryTextBlockStyle}" FontSize="11" MaxWidth="180" TextTrimming="CharacterEllipsis"/></StackPanel>
    </StackPanel>
   </Grid>
  </Border>
  <Border Grid.Row="1" Margin="20,16,20,0" Style="{StaticResource PanelBorderStyle}" Padding="16">
   <Grid x:Name="BarcodeRegion" FocusManager.FocusedElement="{Binding ElementName=BarcodeInput}">
    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="18"/></Grid.RowDefinitions>
    <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
    <TextBlock Text="Barcode" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,14,0"/>
    <TextBox x:Name="BarcodeInput" Grid.Column="1" Text="{Binding BarcodeText, UpdateSourceTrigger=PropertyChanged}" AutomationProperties.Name="Scan or enter barcode"><TextBox.InputBindings><KeyBinding Key="Enter" Command="{Binding ScanBarcodeCommand}"/></TextBox.InputBindings></TextBox>
    <Button Grid.Column="2" Content="Add" Command="{Binding ScanBarcodeCommand}" Style="{StaticResource PrimaryBlueButtonStyle}" MinWidth="84" Margin="10,0,0,0"/>
    <TextBlock Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding BarcodeMessage}" Foreground="{StaticResource DangerBrush}" Margin="0,4,0,0" FontSize="12" AutomationProperties.LiveSetting="Assertive"/>
   </Grid>
  </Border>
  <ContentControl x:Name="CartRegion" Grid.Row="2" Margin="20,16,20,20"/>
 </Grid>
</UserControl>
