<UserControl x:Class="RetailPOS.Desktop.Views.CartPanelView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
 <UserControl.Resources><BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/></UserControl.Resources>
 <Grid>
  <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="16"/><ColumnDefinition Width="280"/></Grid.ColumnDefinitions>
  <Border Style="{StaticResource PanelBorderStyle}">
   <Grid>
    <Grid.RowDefinitions><RowDefinition Height="54"/><RowDefinition Height="*"/></Grid.RowDefinitions>
    <Grid Margin="18,0">
     <StackPanel Orientation="Horizontal" VerticalAlignment="Center"><TextBlock Text="Current Sale" FontSize="19" FontWeight="SemiBold"/><TextBlock Margin="12,0,0,0" VerticalAlignment="Center" Style="{StaticResource SecondaryTextBlockStyle}"><Run Text="{Binding ItemCount, Mode=OneWay}"/><Run Text=" items"/></TextBlock></StackPanel>
     <Button Content="Clear sale" Command="{Binding ClearCommand}" Style="{StaticResource DangerMutedButtonStyle}" HorizontalAlignment="Right" VerticalAlignment="Center" MinHeight="34" Padding="12,4"/>
    </Grid>
    <Grid Grid.Row="1">
     <DataGrid ItemsSource="{Binding Lines}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeRows="False" HeadersVisibility="Column" GridLinesVisibility="Horizontal" RowHeaderWidth="0" RowHeight="58" ColumnHeaderHeight="42" BorderThickness="0,1,0,0" BorderBrush="{StaticResource BorderBrush}" Background="{StaticResource SurfaceBrush}" AlternatingRowBackground="{StaticResource AppBackgroundBrush}" AutomationProperties.Name="Current sale items">
      <DataGrid.Columns>
       <DataGridTextColumn Header="Product" Binding="{Binding ProductName}" Width="*"/>
       <DataGridTextColumn Header="Unit price" Binding="{Binding UnitPrice, StringFormat={}{0:N0} KRW}" Width="130"/>
       <DataGridTemplateColumn Header="Quantity" Width="130"><DataGridTemplateColumn.CellTemplate><DataTemplate><StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
        <Button Content="-" Command="{Binding DataContext.DecreaseQuantityCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding ProductId}" Style="{StaticResource SecondaryButtonStyle}" Width="34" Height="32" MinHeight="32" Padding="0" ToolTip="Decrease quantity"/>
        <TextBlock Text="{Binding Quantity}" Width="42" TextAlignment="Center" VerticalAlignment="Center"/>
        <Button Content="+" Command="{Binding DataContext.IncreaseQuantityCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding ProductId}" Style="{StaticResource SecondaryButtonStyle}" Width="34" Height="32" MinHeight="32" Padding="0" ToolTip="Increase quantity"/>
       </StackPanel></DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn>
       <DataGridTextColumn Header="Discount" Binding="{Binding DiscountText}" Width="84"/>
       <DataGridTextColumn Header="Amount" Binding="{Binding LineTotal, StringFormat={}{0:N0} KRW}" Width="130"/>
       <DataGridTemplateColumn Header="" Width="78"><DataGridTemplateColumn.CellTemplate><DataTemplate><Button Content="Remove" Command="{Binding DataContext.RemoveProductCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding ProductId}" Style="{StaticResource DangerMutedButtonStyle}" MinHeight="32" Padding="9,3" ToolTip="Remove line"/></DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn>
      </DataGrid.Columns>
     </DataGrid>
     <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding IsEmpty, Converter={StaticResource BooleanToVisibilityConverter}}"><TextBlock Text="No products in the current sale." FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center"/><TextBlock Text="Scan a barcode or open Product Search." Style="{StaticResource SecondaryTextBlockStyle}" Margin="0,7,0,0"/></StackPanel>
    </Grid>
   </Grid>
  </Border>
  <Border Grid.Column="2" Style="{StaticResource PanelBorderStyle}" Padding="20">
   <Grid>
    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
    <TextBlock Text="Transaction" FontSize="19" FontWeight="SemiBold"/>
    <StackPanel Grid.Row="1" Margin="0,18,0,0">
     <TextBlock Text="Manual discount" FontWeight="SemiBold" Margin="0,0,0,8"/>
     <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="8"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="6"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
      <TextBox Text="{Binding DiscountInput, UpdateSourceTrigger=PropertyChanged}" ToolTip="Enter a whole KRW amount or percentage rate"/>
      <Button Grid.Column="2" Content="KRW" Command="{Binding ApplyFixedDiscountCommand}" Style="{StaticResource SecondaryButtonStyle}" Padding="9,4"/>
      <Button Grid.Column="4" Content="%" Command="{Binding ApplyPercentageDiscountCommand}" Style="{StaticResource SecondaryButtonStyle}" Width="42" Padding="8,4"/>
     </Grid>
     <TextBlock Text="{Binding DiscountErrorMessage}" Foreground="{StaticResource DangerBrush}" FontSize="12" MinHeight="18" Margin="0,6,0,0" TextWrapping="Wrap"/>
     <Grid Visibility="{Binding HasDiscount, Converter={StaticResource BooleanToVisibilityConverter}}"><TextBlock Text="{Binding DiscountDescription}" Style="{StaticResource SecondaryTextBlockStyle}" VerticalAlignment="Center"/><Button Content="Clear" Command="{Binding ClearDiscountCommand}" Style="{StaticResource SecondaryButtonStyle}" HorizontalAlignment="Right" MinHeight="30" Padding="8,2" FontSize="12"/></Grid>
    </StackPanel>
    <StackPanel Grid.Row="3">
     <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Padding="0,18,0,0"><StackPanel>
      <Grid Margin="0,0,0,10"><TextBlock Text="Subtotal"/><TextBlock Text="{Binding Subtotal, StringFormat={}{0:N0} KRW}" HorizontalAlignment="Right"/></Grid>
      <Grid Margin="0,0,0,14"><TextBlock Text="Discount"/><TextBlock Text="{Binding DiscountAmount, StringFormat={}{0:N0} KRW}" Foreground="{StaticResource SuccessBrush}" HorizontalAlignment="Right"/></Grid>
      <TextBlock Text="Total" Style="{StaticResource SecondaryTextBlockStyle}"/><TextBlock Text="{Binding Total, StringFormat={}{0:N0} KRW}" FontSize="30" FontWeight="Bold" HorizontalAlignment="Right" Margin="0,4,0,18"/>
     </StackPanel></Border>
     <Button Content="Credit Card" Command="{Binding CheckoutCommand}" Style="{StaticResource PrimaryBlueButtonStyle}" Margin="0,0,0,8" AutomationProperties.HelpText="Continue to card payment"/>
     <Button Content="Cash" Command="{Binding CheckoutCommand}" Style="{StaticResource SecondaryButtonStyle}" AutomationProperties.HelpText="Continue to cash payment"/>
    </StackPanel>
   </Grid>
  </Border>
 </Grid>
</UserControl>
