<UserControl x:Class="RetailPOS.Desktop.Views.ProductGridView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:RetailPOS.Desktop.Converters">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:ProductCategoryImageConverter x:Key="ProductCategoryImageConverter"/>
    </UserControl.Resources>
    <Border Style="{StaticResource PanelBorderStyle}" Padding="24">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Text="Products" Style="{StaticResource TitleTextBlockStyle}" />

            <Grid Grid.Row="1" Margin="0,18,0,12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox Text="{Binding BarcodeText, UpdateSourceTrigger=PropertyChanged}"
                         AutomationProperties.Name="Scan barcode">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Enter" Command="{Binding ScanBarcodeCommand}" />
                    </TextBox.InputBindings>
                </TextBox>
                <Button Grid.Column="1"
                        Content="Scan"
                        Command="{Binding ScanBarcodeCommand}"
                        Style="{StaticResource PrimaryBlueButtonStyle}"
                        MinWidth="88"
                        Margin="10,0,0,0" />
                <TextBlock Grid.Row="1"
                           Grid.ColumnSpan="2"
                           Text="{Binding BarcodeMessage}"
                           Foreground="{StaticResource DangerBrush}"
                           Margin="0,8,0,0"
                           Visibility="{Binding HasBarcodeMessage, Converter={StaticResource BooleanToVisibilityConverter}}" />
            </Grid>

            <Grid Grid.Row="2" Margin="0,0,0,22">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         AutomationProperties.Name="Search products">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Enter" Command="{Binding SearchCommand}" />
                    </TextBox.InputBindings>
                </TextBox>
                <Button Grid.Column="1"
                        Content="Search"
                        Command="{Binding SearchCommand}"
                        Style="{StaticResource PrimaryBlueButtonStyle}"
                        MinWidth="88"
                        Margin="10,0,0,0" />
            </Grid>

            <Grid Grid.Row="3">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled"
                              CanContentScroll="True"
                              AutomationProperties.Name="Product results"
                              Visibility="{Binding HasProducts, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel>
                <TextBlock Text="{Binding ProductResultsText}"
                           Style="{StaticResource SecondaryTextBlockStyle}"
                           Margin="0,0,0,10"
                           AutomationProperties.LiveSetting="Polite"/>
                <ItemsControl ItemsSource="{Binding Products}"
                              AutomationProperties.Name="Available products">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="2" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemContainerStyle>
                        <Style TargetType="ContentPresenter">
                            <Setter Property="Margin" Value="0,0,12,12" />
                        </Style>
                    </ItemsControl.ItemContainerStyle>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Command="{Binding DataContext.AddProductCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    CommandParameter="{Binding}"
                                    Style="{StaticResource SecondaryButtonStyle}"
                                    Padding="0" MinHeight="224"
                                    HorizontalContentAlignment="Stretch"
                                    VerticalContentAlignment="Stretch"
                                    AutomationProperties.Name="{Binding Name, StringFormat=Add {0} to cart}"
                                    ToolTip="Click or press Enter or Space to add one to cart">
                                    <Grid>
                                        <Grid.RowDefinitions><RowDefinition Height="116"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                                        <Border Background="{StaticResource AppBackgroundBrush}" CornerRadius="5,5,0,0" ClipToBounds="True">
                                            <Image Source="{Binding CategoryName, Converter={StaticResource ProductCategoryImageConverter}}"
                                                   Stretch="UniformToFill"/>
                                        </Border>
                                        <StackPanel Grid.Row="1" Margin="16,13,16,16">
                                        <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="SemiBold" TextWrapping="Wrap" />
                                        <TextBlock Text="{Binding CategoryName}"
                                                   Style="{StaticResource SecondaryTextBlockStyle}"
                                                   Margin="0,5,0,0" />
                                        <TextBlock Text="{Binding UnitPrice, StringFormat={}{0:N0} KRW}"
                                                   FontSize="18" FontWeight="Bold"
                                                   Foreground="{StaticResource PrimaryBlueBrush}"
                                                   Margin="0,10,0,0" />
                                        </StackPanel>
                                    </Grid>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <Button Content="Load more products"
                        Command="{Binding LoadMoreProductsCommand}"
                        Style="{StaticResource SecondaryButtonStyle}"
                        HorizontalAlignment="Stretch"
                        Margin="0,4,12,12"
                        Visibility="{Binding HasMoreProducts, Converter={StaticResource BooleanToVisibilityConverter}}"
                        AutomationProperties.HelpText="Shows the next 50 matching products"/>
                </StackPanel>
                </ScrollViewer>

                <TextBlock Text="Loading products..."
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Style="{StaticResource SecondaryTextBlockStyle}"
                           Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" />
                <TextBlock Text="No products found."
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Style="{StaticResource SecondaryTextBlockStyle}"
                           Visibility="{Binding IsEmpty, Converter={StaticResource BooleanToVisibilityConverter}}" />
                <TextBlock Text="{Binding ErrorMessage}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Foreground="{StaticResource DangerBrush}"
                           Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}"
                           AutomationProperties.LiveSetting="Assertive" />
            </Grid>
        </Grid>
    </Border>
</UserControl>
